fav lua



local ref = gui.Reference("Misc", "General", "Extra")
local freeze_ragdoll = gui.Checkbox(ref, "misc.fun.freeze_ragdoll", "Freeze Ragdolls", false)

ffi.cdef[[
    void* GetModuleHandleA(const char* lpModuleName);
    void* GetProcAddress(void* hModule, const char* lpProcName);
]]

local FCVAR_CHEAT = 16384

local function GetInterface(dll, name)
    local module = ffi.C.GetModuleHandleA(dll)
    if module == nil then return nil end
    local ci_addr = ffi.C.GetProcAddress(module, "CreateInterface")
    if ci_addr == nil then return nil end
    local ci = ffi.cast("void*(__cdecl*)(const char*, int*)", ci_addr)
    return ci(name, nil)
end

local icvar = GetInterface("vstdlib.dll", "VEngineCvar007")
if not icvar then return end

local find_var_fn = ffi.cast("void*(__thiscall*)(void*, const char*)", ffi.cast("uintptr_t**", icvar)[0][13])

local function SetupConVar(name)
    local ptr = find_var_fn(icvar, name)
    if ptr == nil or ffi.cast("uintptr_t", ptr) == 0 then return nil end
    
    return {
        ptr = ptr,
        SetFloat  = ffi.cast("void(__thiscall*)(void*, float)", ffi.cast("uintptr_t**", ptr)[0][13]),
        RemoveFlags = ffi.cast("void(__thiscall*)(void*, int)", ffi.cast("uintptr_t**", ptr)[0][4])
    }
end

local cv_rcount = SetupConVar("cl_ragdoll_maxcount")
local cv_sleep  = SetupConVar("ragdoll_sleepaftertime")

if cv_rcount then cv_rcount.RemoveFlags(cv_rcount.ptr, FCVAR_CHEAT) end
if cv_sleep  then cv_sleep.RemoveFlags(cv_sleep.ptr, FCVAR_CHEAT) end

local last_state = false

callbacks.Register("CreateMove", "freeze_ragdoll_logic", function(cmd)
    local enabled = freeze_ragdoll:GetValue()
    
    if enabled == last_state then return end
    last_state = enabled

    if enabled then
        if cv_rcount then cv_rcount.SetFloat(cv_rcount.ptr, 0.0) end  
        if cv_sleep  then cv_sleep.SetFloat(cv_sleep.ptr, 0.0) end   
    else
        if cv_rcount then cv_rcount.SetFloat(cv_rcount.ptr, 32.0) end  
        if cv_sleep  then cv_sleep.SetFloat(cv_sleep.ptr, 5.0) end   
    end
end)

callbacks.Register("Unload", function()
    if cv_rcount then cv_rcount.SetFloat(cv_rcount.ptr, 32.0) end
    if cv_sleep  then cv_sleep.SetFloat(cv_sleep.ptr, 5.0) end
end)
